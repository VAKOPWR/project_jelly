import 'package:flutter/material.dart';

class GhostModeTabGroups extends StatelessWidget {
  const GhostModeTabGroups({super.key});

  @override
  Widget build(BuildContext context){
    return Container(
      color: Colors.green,
      child: Text('Test3'),
    );
  }
}